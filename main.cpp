//#include <iostream>
#include <stdio.h>
#include "cipher.h"

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main() {
	utama var[50];
	Enkripsi En[50];
	Dekripsi De[50];
	tampilan(var, En, De);
	return 0;
}